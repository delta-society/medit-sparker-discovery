# KPI 플러그인 적용·검증 — 2026-09-08

사용자 지시: KPI 설정이 어려운 참가자에게 『이익의 아홉 자리』로 KPI를 제안하고 생산력 향상 여부를 확인하도록 교육 플러그인 수정.

## 적용 범위

- 신규 교육 도구나 공동 원장을 만들지 않고 기존 `/sparker-discovery:plan`에 KPI 초안·단일 질문·측정 계획·전후 비교를 연결했다.
- 프레임워크/대화 계약: `plugin/references/kpi-design.md`. 기준값·수치 목표를 모르더라도 지표 정의·품질 조건·측정 계획은 작성한다.
- 기존 DSOS `projects/mbk-medit/decisions.md`의 2026-09-07 「세 과제 생산능력 측정 프레임워크와 프로그램 기틀」과 `artifacts/production-measurement/README.md`의 동일 품질 산출당 총인시간 계약을 따른다. 교육 도우미는 그 역수인 시간당 품질 통과 산출을 계산한다. 수요함수 네 통로는 업무 인과관계를 설명하는 참고 후보이며 공통 성과 판정을 매출·가격 중심으로 바꾸지 않는다. 기존 측정 웹 프로그램·DB를 대체하거나 자동 연동하지 않았다.
- `plugin/scripts/kpi.py`: 표준 라이브러리만으로 출처·기간·같은 단위/난도/품질/시간범위, 결측과 0, 자기보고·실측·합성, 품질 저하, 변화 없음·감소·증가를 구분한다. 본체는 신뢰된 읽기 전용 계산이며 임의 업무 코드를 실행하지 않는다.
- `plan.kpi`는 기존 기획서의 선택 확장이다. KPI가 없던 리비전은 byte 단위 보존하고 KPI는 새 리비전 JSON/한국어 Markdown에 함께 저장·재조회한다. 과거 확정본의 재개 규칙은 유지한다.
- 노동생산성 관찰을 전사 생산능력·총요소생산성·AI 인과효과·이익 실현으로 승격하지 않는다. 품질 통과율 하락 시 빠른 처리만으로 성공 판정하지 않는다. 관측 출처 문자열만으로 사실을 독립 검증했다고 주장하지 않는다.

## 실행 증거

- `python3 -m unittest discover -s plugin/tests -v`: 38 tests PASS. 신규 KPI 시험과 기존 AR/ST·저장/확정 회귀 포함.
- 구버전 호환 fixture는 수정 전 0.1.2 ZIP의 실제 `plan.py`를 실행해 생성했다. 기존 Markdown을 그대로 읽고 KPI 확장 리비전을 저장·재조회하는 시험 PASS.
- `claude plugin validate plugin`: PASS.
- **최종 ZIP 실제 Claude Code 대화 3회 및 저장 재조회 PASS.** 기준값 미확인 ST 업무에서 2개 KPI·측정 방법을 제안하고 미확인 값을 그대로 저장했다. 합성 전후 비교는 시간당 통과 산출 9→18(+100%)을 실제 성과와 구분했다. 후속 통과 건수 정정 90→70에서는 시간당 산출이 14로 증가해도 품질 통과율 90%→70% 하락으로 성공 판정을 거부했다. 두 리비전과 정정 출처 보존·재조회 확인.
- 검증 명령: `python3 scripts/verify-kpi-native.py /Users/Shared/DeltaSocietyOS/audit/sparker-kpi-20260908 /Users/hermes/deliverables/sparker-discovery-plugin.zip` (exit0). 원본 대화·저장본·검증 영수증은 위 audit 경로에만 보존한다.
- ZIP 0.2.0 SHA256: `95c693d92d3a4ad46343f5aee632b8c8779a43e2538b94cb0909da094ee5628c`. 네이티브 시험에 사용한 파일과 전달본 동일성 확인.
- 관측된 한계: 미확인 ST 대화에서 소문자 ID 계약 위반을 저장 검증기가 거부했고 모델이 재작성해 저장을 완료했다. 모든 첫 시도가 오류 없이 끝났다는 주장은 하지 않는다. 실제 참가자의 실습 소요시간·범용 업무 전체·고객 원천 자동 연결은 검증 범위 밖이다.

참가자 실제 생산력 향상은 아직 측정하지 않았다. 이번 증거는 도구의 계산·대화·저장 동작이며 교육 인도 완료나 고객 성과 확인이 아니다.
